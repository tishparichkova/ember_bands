<div>
    {{#each bands as |band|}}
        {{#link-to "bands.songs" band.name}}
          <p class="test-tihomira">
              {{band.name}}
          </p>
          <p>
              <img src="{{band.image}}" alt="{{band.name}}">
          </p>
        {{/link-to}}
    {{/each}}
</div>
{{outlet}}