<div {{action "sendAction" band.id}} class="band-container">
  <p class="band-container__band-title">
    {{band.name}}
  </p>
  <div class="band-container__band-image-holder">
    <img class="band-image-holder__image" src={{band.image}} alt={{band.name}}>
  </div>
</div>